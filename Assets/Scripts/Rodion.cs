using System;
using UnityEngine;

public class Rodion : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello, World from Rodion!");  //Rodion
        Debug.Log("Hello, World! RodionHomePC");   //RodionHomePC
    }

    // Update is called once per frame
    void Update()
    {

    }
}
