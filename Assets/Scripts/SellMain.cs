using UnityEngine;

public class SellMain : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("awdw");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
