using UnityEngine;

public class Zahar : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello i am Zahar!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
